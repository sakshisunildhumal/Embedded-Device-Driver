#include "operations.h" 
#include <math.h> 
#include <stdio.h>

float sin1(float x) 
{
    float result = sin(x);
    return result;
}

float cos1(float x) 
{
    float result = cos(x);
    return result;
}

float tan1(float x) 
{
    float result = tan(x);
    return result;
}
